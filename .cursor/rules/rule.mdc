---
alwaysApply: true
---
# 项目概述

这是一个开源的IM（即时通讯）项目，对标大厂和开源IM产品。

### 技术架构
Flutter + Dart

### 目录结构
```
lib/
├── app/              # 应用入口
├── api/              # API 接口定义
├── common/           # 通用工具和配置
│   ├── cache/        # 缓存工具
│   ├── config/       # 配置文件
│   ├── logger/       # 日志工具
│   ├── request/      # HTTP 请求工具
│   └── websocket/    # WebSocket 工具
├── core/             # 核心业务逻辑
│   ├── database/     # 数据库操作
│   ├── datasync/     # 数据同步
│   ├── message/      # 消息管理
│   └── theme/        # 主题配置
├── di/               # 依赖注入
├── features/         # 功能模块
│   ├── auth/         # 认证模块
│   │   ├── login/    # 登录路由
│   │   │   ├── bloc/      # 登录状态管理
│   │   │   ├── data/      # 登录数据
│   │   │   ├── components/  # 登录相关组件
│   │   │   └── login_page.dart      # 登录页面
│   │   └── register/ # 注册路由
│   │       ├── bloc/      # 注册状态管理
│   │       ├── data/      # 注册数据
│   │       ├── components/  # 注册相关组件
│   │       └── register_page.dart   # 注册页面
│   ├── chat/         # 聊天模块
│   │   ├── chat_list/    # 聊天列表路由
│   │   │   ├── bloc/      # 状态管理
│   │   │   ├── data/      # 数据
│   │   │   ├── components/  # 组件
│   │   │   └── chat_list_page.dart  # 聊天列表页面
│   │   └── chat_detail/  # 聊天详情路由
│   │       ├── bloc/      # 状态管理
│   │       ├── data/      # 数据
│   │       ├── components/  # 组件
│   │       └── chat_detail_page.dart  # 聊天详情页面
│   ├── contact/      # 联系人模块
│   ├── home/         # 首页模块
│   ├── moment/       # 动态模块
│   └── user/         # 用户模块
│       ├── mine/      # 个人中心
│       │   ├── bloc/      # 状态管理
│       │   ├── data/      # 数据
│       │   ├── components/  # 组件
│       │   └── mine_page.dart  # 个人中心页面
│       ├── profile/    # 个人资料
│       │   ├── bloc/      # 状态管理
│       │   ├── data/      # 数据
│       │   ├── components/  # 组件
│       │   └── profile_page.dart  # 个人资料页面
│       ├── qrcode/    # 二维码
│       │   ├── bloc/      # 状态管理
│       │   ├── data/      # 数据
│       │   ├── components/  # 组件
│       │   └── qrcode_page.dart  # 二维码页面
│       └── config/     # 用户配置
│           ├── bloc/      # 状态管理
│           ├── data/      # 数据
│           ├── components/  # 组件
│           └── config_page.dart  # 用户配置页面
├── router/           # 路由配置
└── shared/           # 共享组件和工具
    ├── ui/           # UI组件库
    │   ├── avatar/   # 头像组件
    │   ├── button/   # 按钮组件
    │   ├── dialog/   # 对话框组件
    │   ├── header/   # 头部组件
    │   ├── image/    # 图片组件
    │   ├── layout/   # 布局组件
    │   ├── menus/    # 菜单组件
    │   └── toast/    # 提示组件
    ├── utils/        # 通用工具
    └── widgets/      # 业务组件
```
### ui规范
- 尽可能使用ui组件中的layout作为骨架屏
- 所有页面都需要使用ui组件中的layout作为骨架屏
- 如果组件库中有的组件可以直接使用，就直接使用，不需要自己实现
- ui组件在`shared/ui/`目录下，每个组件都有一个对应的目录，目录名就是组件名。每个目录下都有一个`index.dart`文件，文件中就是组件的实现
- 对于简单的确认对话框，应使用`BeaverDialog`组件；对于复杂的业务表单弹窗，应创建独立的业务组件
- 所有提示信息应使用`BeaverToast`组件，避免使用Flutter自带的`ScaffoldMessenger`
- 所有头像显示应使用`BeaverAvatar`组件

### components层规范
- 每个功能模块可以创建`components`目录，用于存放该模块的业务组件
- components层的组件应该是具有完整业务逻辑的独立组件，而不仅仅是UI渲染
- 组件内部应该处理自己的状态和业务逻辑，减少对父组件的依赖
- 组件之间通过明确的参数和回调函数进行通信
- 主入口文件（如`profile_page.dart`）应该保持简洁，主要负责组件的组合和页面布局
- components不是必须的，但当主入口文件过大时（超过500行），建议进行拆分
- 拆出去的组件也是业务组件，应包含完整的业务逻辑，不要只拆出去UI部分然后将数据传入，结果又在父组件中处理业务逻辑

### UI适配规范
- UI适配基准宽度为 **375**（逻辑像素）。
- 开发时请使用 `flutter_screenutil`，对于尺寸（宽高、边距、字体等）直接使用对应的 `.w` 或 `.sp`。例如，设计稿标为 16px，使用 `16.w` 或 `16.sp` 即可，不需要手动除以 2。

### 文件命名规范
- 所有文件推荐使用下划线的方式命名（lowCamelCase）。如果就一个单词不需要使用下划线。
- 如果文件夹名已包含了某个功能前缀（如 `user`），则子文件不再重复该前缀。
- **禁止在目录名中包含重复的功能前缀**，例如在 `api` 目录下创建 `_api` 或 `api_` 开头的文件，或创建 `xxApi` 或 `apiXXX` 命名的文件。目录名本身已经表达了功能，文件名不应重复。

### 文件与路径
- 不要编写 .md 文档。
- 尽可能使用包路径（如 `package:beaver/...`），避免使用 `../` 之类的相对路径。
- 全局共享的服务或模块，可以在目录下创建导出文件（如 `index.dart`），便于统一导入。
- 对于功能模块的子目录（如 `datasync/chat/`），不需要创建导出文件，直接导入具体文件即可。

### 接口规范
- 数据结构统一为：
```dart
class BaseResponse<T> {
  final int code;
  final String msg;
  final T? result;
  
  BaseResponse({required this.code, required this.msg, this.result});
}
```
- 接口类型以 `I` 开头，以 `Req` 或 `Res` 结尾。
- 由于底层已统一处理异常，业务代码中严禁写 `try...catch`，直接根据 `code == 0` 判断成功，非 0 则使用 `msg`。

### 资源管理
- 严禁使用文本表情包（如 📞、✓），统一使用 SVG 文件。
- SVG 存放路径：`assets/images/[module]/[name].svg`。

### 状态管理
- **功能模块状态 (Feature-Level)**: 使用 `flutter_bloc` 的 BLoC 模式处理复杂 UI。
  - 事件 (Event): 表示用户操作或系统事件。
  - 状态 (State): 表示 UI 的当前状态。
  - BLoC: 处理事件并生成新状态。

- **全局状态存储 (Global Store)**: 对标桌面端 Pinia，使用 `Cubit` 实现轻量化全局存储。
  - **目录结构**: 统一存放在 `lib/store/[module]/[module].dart`。
  - **单文件规范**: Store 类与对应的 State 类应定义在同一个文件中。
  - **实现模式**:
    - **State 类**: 继承 `Equatable`，通过 `copyWith` 实现不可变更新。
    - **Store 类 (Cubit)**: 通过直接调用方法（类似 Pinia Actions）修改状态，无需定义 Event。
  - **同步机制**: 优先监听 `business` 层提供的 `Stream` 进行响应式更新，确保全应用数据一致。

### 代码风格
- 保持代码精简，拒绝过度开发。
- 严格遵循 Dart 语言规范和 Flutter 最佳实践。
- 使用 `dart format` 保持代码格式化一致。

### 依赖与版本规范
- **包管理**: `pubspec.yaml` 中的所有依赖包必须保持最新稳定版本。获取依赖：`fvm flutter pub get`，更新依赖：优先使用 `fvm flutter pub upgrade --major-versions`。
- **运行应用**: 开发调试优先使用 `fvm flutter run -d emulator-5554`。
- **API 版本**: 所有 API 接口请求和数据结构定义必须对标 `beaver-desktop` 或服务端最新的定义，确保字段和逻辑的 1:1 同步。
- **代码生成**: 在修改数据库表定义（Table）或 JSON 模型后，必须运行 `fvm flutter pub run build_runner build --delete-conflicting-outputs` 更新生成的代码（`.g.dart`）。

### 数据库规范
- **与后端对应**: 本地数据库结构必须与后端数据库保持 1:1 对应，包括表结构、字段名称、字段类型等。
- **表结构对标**: 数据库表定义必须参考 `beaver-desktop` 项目中的数据库表结构，确保字段和逻辑完全一致。
- **命名规范**: 数据库表名、字段名应与后端保持一致，使用下划线命名法。
- **迁移管理**: 数据库版本升级时，必须通过迁移脚本（migration）处理，确保数据的平滑迁移。
- **服务层实现**: 数据库服务层（services）的方法应与 `beaver-desktop` 中的实现保持一致，确保业务逻辑的统一性。
- **类型定义**: 数据库相关的类型定义应使用 Drift 生成的模型类，位于 `lib/core/database/tables` 目录下，不应手动创建类型定义文件。

### 架构分层规范 (Architecture Layering)

为了保证 IM 复杂业务下的数据一致性和代码可维护性，必须严格执行以下分层逻辑：

1.  **UI 层 (features/)**:
    *   职责：页面渲染与交互。
    *   状态管理：使用 BLoC 处理局部 UI 逻辑，或监听全局 Store。
    *   限制：禁止直接调用 `AppDatabase`、`api` 或 `datasync`。**必须通过 `business` 层或 `store` 层获取数据。**

2.  **全局 Store 层 (store/)**:
    *   职责：充当响应式数据枢纽，对标 Pinia。存储 User、Contact、Chat 等全局共享状态。
    *   **同步逻辑**：通过监听 `Business` 层的 `Stream` 实现全应用实时同步。

3.  **业务门面层 (core/business/)**:
    *   职责：作为 UI/Store 的唯一入口。聚合多个 `services` 和 `datasync` 逻辑。
    *   功能：负责读写分离、缓存优先策略、离线发送逻辑等。
    *   **依赖**：依赖 `core/database/services`、`core/datasync` 和 `api`。

4.  **持久化层 (core/database/services/)**:
    *   职责：底层的 CRUD 操作。
    *   **限制**：仅由 `business` 或 `datasync` 调用。

5.  **数据同步层 (core/datasync/)**:
    *   职责：负责与后端进行数据版本比对、增量拉取。
    *   **限制**：仅由 `business` 层触发增量同步。

### 数据流说明

#### 1. 读操作 (Read Flow)
```
UI (Bloc) -> Global Store (In-Memory) -> UI (Reactive Update) -> Global Store -> Business -> Database (Cache) -> Global Store -> UI
```

#### 2. 写操作 (Write Flow)
```
UI -> Business -> API -> Local Database -> Business (Stream) -> Global Store -> UI (All pages Listening)
```

#### 3. 依赖注入规范 (DI)
*   所有 `Business` 类必须在 `lib/di/injection.dart` 中注册为 `LazySingleton`。
*   所有 `Service` 类必须在 `lib/di/injection.dart` 中注册并由 `Business` 层通过 `getIt` 获取。

### 依赖注入规范 (Dependency Injection)

#### 目录结构
```
lib/di/
├── core/                    # 核心基础设施配置
│   ├── network.dart         # 网络相关依赖
│   ├── database.dart        # 数据库相关依赖
│   └── services.dart        # 数据服务层依赖
├── business/                # 业务层配置
│   ├── user.dart            # 用户业务配置
│   ├── friend.dart          # 好友业务配置
│   ├── group.dart           # 群组业务配置
│   ├── chat.dart            # 聊天业务配置
│   ├── message.dart         # 消息业务配置
│   ├── notification.dart    # 通知业务配置
│   ├── moment.dart          # 动态业务配置
│   └── call.dart            # 通话业务配置
├── modules.dart             # 模块注册
└── injection.dart           # 主入口文件
```

#### 注册规则
- **核心层**：网络、数据库、服务等基础设施使用 `registerLazySingleton` 注册
- **业务层**：Business类和接口使用 `registerLazySingleton` 注册
- **懒加载机制**：所有注册都使用懒加载，只有在第一次被请求时才初始化

#### 需要注册的组件
1. **网络层**：`HttpClient`、`WsConnectionManager`
2. **数据库层**：`AppDatabase`
3. **服务层**：所有 `Service` 类（如 `UserService`、`FriendService` 等）
4. **业务层**：所有 `Business` 类（如 `UserBusiness`、`FriendBusiness` 等）
5. **接口实现**：业务接口与实现的绑定（如 `UserRepositoryInterface` → `UserBusiness`）

#### 不需要注册的组件
1. **页面Widget**：纯展示性组件
2. **BLoC**：由页面创建或通过 `BlocProvider` 提供
3. **Repository**：由BLoC创建或通过构造函数注入
4. **数据模型**：纯数据类
5. **工具类**：无状态工具函数

#### 性能优化
- 使用懒加载避免启动时的性能问题
- 服务只在需要时初始化，不会占用额外内存
- 避免循环依赖，确保依赖关系清晰

### 切记
- 严格遵循文件命名和路径规范。
- 保持代码结构清晰，职责分离。
- 优先使用现有工具和组件，避免重复造轮子。