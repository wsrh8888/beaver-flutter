## 建议
1. 不要使用list来存储数据,并发不够
2. 端到端会安全点 - 调研
3. 

1、fvm flutter run -d emulator-5554
2、打包命令：
   - Android APK: `fvm flutter build apk --release`
   - Android AppBundle: `fvm flutter build appbundle --release`
   - iOS: `fvm flutter build ipa --release`
   - Web: `fvm flutter build web --release`
